baseURL = "https://commons.engineering/"
languageCode = "en"
title = "Commons Engineering"

[params]
  description = "A discipline for designing living systems toward vitality rather than extraction"
  author = "higgerix"
  license = "CC-BY-SA-4.0"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 4

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]
